{
  "name": "sass-file",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "sass:watch": "sass --no-source-map --watch src/style.scss style.css",
    "sass:compile": "sass --no-source-map src/style.scss style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.5"
  }
}